# Sample TOML for toml_get tests
[auth.email.template.confirmation]
subject = "Confirm your account"
content_path = "./confirmation.html"

# Use dotted keys for recovery
auth.email.template.recovery.subject = "Reset your password"
auth.email.template.recovery.content_path = "./recovery.html"

[auth.email.template.magic_link]
subject = "Magic link sign-in" # comment
content_path = "./magiclink.html"
